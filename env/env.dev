VITE_APP_ENVIRONMENT="development"
# burakov_api origin the app calls in local dev (signaling WebSocket + ICE creds).
VITE_API_URL="http://localhost:3000"
# App identity in the shared backend's project registry (seeded in burakov_api).
VITE_CLIENT_ID="sidedrop"
